import { registerRootComponent } from 'expo';
import App from './App';

// registerRootComponent tells Expo to start rendering your application
// at the top level of your native platform's tree.
registerRootComponent(App);
